import React from 'react';

function FilterName() {
  return (
    <input
      type="text"
      data-testid="name-filter"
      value="{}"
      onChange="{}"
    />
  );
}

export default FilterName;
